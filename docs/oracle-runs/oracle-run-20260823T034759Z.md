# Oracle backfill run — Round 529 (solo autonomous cron round)

**Scope:** one round, zh, `--new`. Writes went directly to **production**.
**Batch (12):** 信, 书信, 信心, 信用, 口信, 可信, 电信, 相信, 自信, 通信, 信用卡, 自信心
**Backup (§2):** `server/backups/det-20260823T031732Z-r529.sql.gz` (17M) — taken before any write.

## Result

All 12 rows cleared the full zh manifest and were promoted by
`promote-discoverable.js --apply` (12 promoted, 0 not-ready). Promotion was the last act.

| Script | Rows |
|---|---|
| backfill-tones (`--stale`) | 5 |
| backfill-numbered-pinyin (`--stale`) | 12 |
| dictionary-breakdown | 11 |
| process-definitions-array | 2 updated (critic confirmed 7 unchanged) |
| parts-of-speech | 12 (all accepted first pass) |
| word-forms | 12 |
| hsk-level | 12 |
| frequency-score | 12 |
| cluster-definitions | 12 (18 clusters) |
| long-definitions | 12 |
| longdef-citations | 0 prompts |
| example-sentences | 12 |
| breakdown-senses | 11 |
| breakdown-elaboration | 11 judged, 1 elaboration written (信用) |
| classifier | 12 |

`backfill-icons` skipped (opt-in, needs the paid icons8 API).

## Verification (§5)

- Null-column check on all 12 newly-discoverable rows: `partsOfSpeech`, `longDefinition`,
  `exampleSentences`, `frequencyScore`, `definitionClusters`, `difficulty` all non-null.
  `breakdown` is null on 信 only — correct, the step's `when` is multi-char words.
- `discoverable_without_level` = **0**.
- Validator-guard query (approved/flagged fields touched after run start): **0 rows**.

## Self-flagged review items

`⚠ CLUSTER REVIEW` (4, from cluster-definitions): 信 ×2, 信用 ×1, 通信 ×1.

`⚠ BREAKDOWN SENSE REVIEW` (4):
- **信用** — 用: closer to "being put to use / relied upon" than the plain verb "to use"; no candidate label fits exactly.
- **信用卡** — 用: inherited from 信用 "credit" rather than used independently.
- **相信** — 相: the "mutually" force has bleached away in modern usage; the label is etymologically right but not semantically active.
- **电信** — 电: 电信 covers electrical/electronic transmission generally; "telegram, phone call" is the other plausible reading.

## Findings carried forward

1. **Unstamped-but-populated tones / numbered-pinyin** (third round running: 527, 528, 529).
   The planner lists the step; the script reports "0 needing"; `--stale` is required.
   Worth a one-off stamping sweep rather than paying `--stale` per batch forever.
2. **`--new` frontier hygiene** (standing finding). ~30 candidates dropped by hand —
   explicit/slur headwords and cedict place-name/brand stubs that the curation filter
   in `CHAR_FREQ_CTE` does not catch.
3. **Round-71 word-forms defect still live**: forms derive from the FIRST definition only.
   For 信 (letter / to trust) that would have shipped "lettered"/"letters" verb forms;
   authored noun-only forms instead.
4. **Session budget**: `seven_day` utilization read 100% (critical) with extra-usage credits
   enabled; requests continued to succeed, as in rounds 527/528.

## Quality caveat (inherent to oracle mode)

Answers were authored and self-reviewed by the same agent. The gate is that every answer
passed the unmodified script validators — and it did bite twice this round: the 书信 long
definition was rejected for `comments_on_register`, and 信用 example sentence #1
(皇帝很信用这位大臣) was rejected as `unnatural_phrasing` (the classical verb 信用 does not
take a degree adverb). Both were re-authored.

## Stop

Stopped cleanly after one round, per the invocation. No parked-run state; no
`oracle-resume.md` written.
