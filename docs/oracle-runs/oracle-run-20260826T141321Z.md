# Oracle backfill run — 2026-08-26T14:13:21Z (solo cron)

Invocation: autonomous cron round (solo), unsharded (no `--shard`). Standing
instruction for this invocation: park state to `server/logs/oracle-resume.md`
and notes to `server/logs/oracle-run-notes.md` (shared paths, owned by a
parallel worker); complete exactly one round then stop.

## Backup

`server/backups/det-20260826T131723Z-oracle-cron-solo.sql.gz` — taken before
any writes this session.

## Batch

20-word zh batch (清关 cluster), all promoted to `discoverable = TRUE`:

清关, 无关, 生死关头, 白血球, 白血病, 相关, 相关性, 红血球, 经血, 网关,
自治机关, 血, 血口, 血性, 血战, 血族, 血书, 血本, 血气, 血水

## Per-script results

| Script | Scope | Result |
|---|---|---|
| `chinese/backfill-classifier.js` | 18/20 (nounPos-eligible; excludes 无关, 相关) | 18 updated, 0 failed |
| `chinese/backfill-breakdown-senses.js` | 19/20 (multi-char; excludes 血) | 19 updated, 0 failed, 2 review notes |
| `chinese/backfill-breakdown-elaboration.js` | 19/20 (same set) | 4 elaborated, 15 straightforward, 0 undecided, 0 failed |
| `chinese/backfill-tones.js` (`--stale`) | 13/20 (multi-word subset) | 13 stamped, deterministic, no prompts |
| `chinese/backfill-numbered-pinyin.js` (`--stale`) | 20/20 | 20 stamped, deterministic, no prompts |
| `promote-discoverable.js --apply` | 20/20 | Considered: 20, Promoted: 20, Already discoverable: 0, Not ready: 0 |

Other manifest steps for this batch (parts of speech, definitions, frequency
score, long definition, example sentences, word forms) had already landed in
an earlier session before compaction and were not re-run.

## Finding: tone / numberedPinyin stamp gap

`backfill-tones.js` and `backfill-numbered-pinyin.js` are `deterministic: true,
when: 'always'` in the manifest. Their own `doneGate` (`tone IS NULL` /
`numberedPinyin IS NULL`) found 0 rows needing work — the columns were already
populated (likely by data import) but never stamped in `enrichmentLog`. Since
`oracle-plan.js` and `promote-discoverable.js`'s completeness check
(`buildCompletePredicate` in `server/scripts/backfill/shared/lib/requiredScripts.js`)
is stamp-based, not value-based, this would have silently blocked promotion
despite the underlying data being correct. Both scripts document a `--stale`
flag for exactly this case; running each with `--stale --words=<batch>`
stamped every already-populated row with no AI/oracle prompts required. Worth
checking for on any future batch where a deterministic step reports 0 pending
rows but the planner still lists it.

## Post-promotion verification (skill §5)

All 20 rows confirmed: `discoverable = true`; non-null `partsOfSpeech`,
`longDefinition`, `exampleSentences`, `frequencyScore`; `difficulty` populated
(range 3–6 across the batch); `discoverable_without_level` sanity count = 0.

## Cluster / sense review flags

Carried forward from the earlier (pre-compaction) portion of this session's
batch review — `⚠ CLUSTER REVIEW`:
- 相关 (id=77354)
- 血 (id=96919)
- 血性 (id=96947)
- 血气 (id=96965)

New this window — `⚠ BREAKDOWN SENSE REVIEW` (self-flagged component
disambiguation, not a content-policy concern):
- 生死关头: 头 — idiomatic "moment/juncture" sense vs. literal "head"
- 自治机关: 关 — institutional "agency/office" sense vs. "checkpoint/pass"

## Content policy

No drops this round. All 20 words are ordinary vocabulary (blood terminology,
customs/gateway terms, relatedness terms) with no slur or explicit-content
concerns.

**Deferred, not processed this round** (explicit stop-after-one-round
instruction): the planner's `--new` query keeps resurfacing the same 5
candidates, each already logged many times in `server/logs/oracle-concerns.md`
as drops — 阿三, 小日本, 台巴子, 老黑 (ethnic/racial slurs, primary/only
sense), 日人民报 (vulgar 肏-pun on 人民日报). See that file for the full
recurrence history and the standing recommendation (not actioned this round,
out of scope for a solo run) that these get a durable exclusion — e.g. a
poison-list table or a manual non-discoverable-with-reason flag — instead of
being re-derived and re-dropped by every future round.

## Round status: COMPLETE

Per the standing instruction, no second batch was started this invocation.
