# Oracle backfill run — 2026-08-25T15:24:53Z

Autonomous cron round, solo, unsharded. Scope: finish and promote the in-flight
zh 20-word batch from earlier sessions in this run; stop after one round per the
invocation's explicit instruction (no second `--new`/`--discoverable` batch).

Backup: `server/backups/det-20260825T141722Z-oracle-cron-20260825.sql.gz` (taken earlier
in this run, still current — no schema/data risk introduced since).

## Batch: zh, 20 words

单日,单用,单相,单眼,单眼皮,单眼相机,单县,单色,单行,单行本,单行线,单行道,单语,单身,单车,
单车族,单过,单体,单点,报单

All 20 promoted to `discoverable = TRUE` this round. Verified against
`dictionaryentries_zh`: every row has non-null `partsOfSpeech`, `longDefinition`,
`exampleSentences`, `numberedPinyin`, `frequencyScore`, `definitionClusters`, `breakdown`;
`difficulty` in [3,6]; `discoverable_without_level` = 0; 0 `validations` rows on any of
these ids.

| word | id | difficulty | classifier |
|---|---|---|---|
| 单日 | 22697 | 4 | — |
| 单用 | 22717 | 5 | — |
| 单相 | 22719 | 6 | — |
| 单眼 | 22721 | 5 | 只 |
| 单眼皮 | 22722 | 4 | — |
| 单眼相机 | 22723 | 5 | 台 |
| 单县 | 22736 | 6 | — |
| 单色 | 22742 | 4 | — |
| 单行 | 22748 | 4 | — |
| 单行本 | 22749 | 5 | 本 |
| 单行线 | 22750 | 4 | 条 |
| 单行道 | 22751 | 4 | 条 |
| 单语 | 22757 | 5 | — |
| 单身 | 22763 | 3 | — |
| 单车 | 22768 | 3 | 辆 |
| 单车族 | 22769 | 5 | — |
| 单过 | 22774 | 4 | — |
| 单体 | 22790 | 6 | — |
| 单点 | 22791 | 4 | — |
| 报单 | 25949 | 5 | 张 |

## Dropped words (not part of this batch, not touched)

阿三, 小日本, 台巴子, 老黑, 日人民报 — dropped in earlier sessions of this run per §3c
(ethnic/racial slurs as sole sense; 日人民报 a vulgar pun on a state-newspaper name).
Logged to `server/logs/oracle-concerns.md`; remain the only pending `--new` zh work as of
this round's final planner check.

## Per-script summary (this window)

Scripts converged in earlier sessions of this run (generator, validator-agent,
example-sentences segment-tagger, classifier, breakdown-senses, breakdown-elaboration) —
see prior context; not re-litigated here.

**`chinese/backfill-numbered-pinyin` (v1, always)** — closed out this window. The planner's
`--new` output listed this script as pending for most of the batch, but it performs no AI
call (pure diacritic→number string conversion) and its own `doneGate`
(`numberedPinyin IS NULL`) found 0 rows needing it — the column was already correct. The gap
was a missing `enrichmentLog` stamp: `promote-discoverable.js`'s completeness predicate
requires the version stamp, so it initially reported all 20 as `not ready`. Resolved by
running the script directly with `--stale`, scoped to the 20-word batch, which re-stamps
regardless of the `IS NULL` gate. No data was ever wrong; this is the same
stamp/reality-desync bug class already documented for `spanish/backfill-example-sentences`
in an earlier round in `server/logs/oracle-run-notes.md` — worth a generic fix (stamp on
the no-op/skip path too) but out of scope for a solo enrichment pass.

**`promote-discoverable.js --apply`** — 20/20 promoted, 0 not-ready, 0 already-discoverable.

## Uncertainty flags for review

- **Breakdown-sense review** (self-flagged by `backfill-breakdown-senses.js`, not blocking):
  - 单县 — 单 read as the place-name character Shàn; no offered candidate sense covered that
    reading, best-guess picked.
  - 单点 — 点's "à la carte / to order" sense not covered by the offered "point, dot"
    candidate; best-guess picked.
- **Elaborated words** (4 of 20 judged non-transparent by `backfill-breakdown-elaboration.js`):
  单眼相机, 单县, 单行本, 单点 — see `server/logs/oracle-run-notes.md` for the authored text.
- **Cluster-review flags** (from an earlier session in this run): 单行, 单行本.
- Earlier GSA/candidate-sense quirks noted elsewhere in this run (南开 merge, 今年三十
  mis-split, the "开" candidate-sense mismatch in prompt `22cb0cb433148a79`) belong to other
  batches, not this 20-word set — recorded here only as a pointer since they were flagged
  during the same overall run.

None of the above rose to a §3c drop or an `oracle-concerns.md` entry — all are
disambiguation edge cases with a defensible best-guess answer, not content-policy issues.

## Stop

Per this invocation's explicit instruction, the round ends here. No second `--new` or
`--discoverable` batch was started. `server/logs/oracle-resume.md` is empty — nothing
parked.
