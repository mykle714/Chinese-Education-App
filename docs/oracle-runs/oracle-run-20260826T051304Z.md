# Oracle Backfill Run — 2026-08-26T05:13:04Z

Autonomous cron round (solo, unsharded). One round only, per standing override
instructions — no second `--new` batch started after this one.

## Session budget

Not independently re-checked this segment (continuation of an in-flight round);
no stop-condition (95%+ five_hour, 100% weekly) was hit during this segment.

## Backup

```
server/backups/det-20260826T041716Z-cron-solo.sql.gz
```

Restore:
```bash
gunzip -c server/backups/det-20260826T041716Z-cron-solo.sql.gz | \
  docker exec -i cow-postgres-prod psql -U cow_user -d cow_db
```

## Batch — 20 words newly marked `discoverable`

```
运行时, 运转, 运道, 运量, 一血, 下关市, 中阳, 中阳县, 主管机关, 之内,
事关, 事关重大, 信阳市, 内, 内中, 内人, 内传, 内内, 内出血, 内化
```

All 20 promoted via `promote-discoverable.js --apply` (dry-run and apply both
reported `Considered: 20 | Promoted: 20 | Not ready: 0`).

## Per-script results

| Script | Prompts | Updated | Errors/Failed |
|---|---|---|---|
| `backfill-parts-of-speech` / clusters / long-def / example-sentence generator+validator (earlier this run, prior to this segment) | — | 20/20 | 0 |
| `backfill-example-sentences.js` Stage 3 (per-segment metadata) | 60 (3 cycles × 20) | 20/20 (final cycle) | 0 |
| `backfill-classifier.js` | 18 (17 + 1 fix for an initial 运量 omission) | 18/18 | 0 |
| `backfill-breakdown-senses.js` (19 multi-char words; 内 excluded, single-char) | 17 (2 words auto-completed with no new prompt) | 17/17 (+2) | 0 |
| `backfill-breakdown-elaboration.js` (19 multi-char words) | 19 | 19/19 (6 elaborated, 13 straightforward/NULL) | 0 |
| `backfill-tones.js` | deterministic, non-AI | 15 rows re-stamped (`--stale`) | 0 |
| `backfill-numbered-pinyin.js` | deterministic, non-AI | 20 rows re-stamped (`--stale`) | 0 |

Final `oracle-plan.js --words=<batch>` re-run: **0 prompts across 0 scripts** —
clean confirmation the batch had no remaining manifest work before promotion.

## tone / numberedPinyin planner discrepancy (resolved)

`oracle-plan.js` reported 15 and 20 words respectively as still pending these two
deterministic steps, while both scripts' own `doneGate` (run directly) reported 0
pending, and a direct DB query confirmed `tone`/`numberedPinyin` were already
non-NULL for all 20 rows. This was a **version-stamp gap, not a data gap**: the
columns held correct values (likely populated by an earlier/different code path)
but were never stamped by these scripts. Re-running both with `--stale` recomputed
and re-wrote the same values while correctly writing the manifest-version stamp
(15 and 20 rows respectively "updated"), which cleared the planner's false
positive without altering any data. Recommendation: this pattern — real data
present but unstamped — is worth checking for elsewhere in the corpus rather than
being unique to this batch.

## Guardrail evidence

- Two validator-style rejections earlier this run (内人, 内内 — register-commentary
  violations caught and re-authored) demonstrate the validator/apply cycle
  correctly rejecting non-conforming oracle answers rather than writing them
  through.
- Validator-approval-protection check (§5): 0 pre-existing `validations` rows for
  any of the 20 words — nothing to protect, nothing overwritten.
- Discoverable-without-valid-difficulty check (§5): 0 rows.

## Cluster / breakdown-sense review flags

- **内传** — flagged earlier this run for a 2-cluster split (biography sense vs.
  internal-account sense); carried forward for future review.
- **backfill-breakdown-senses** uncertain picks (5, all with `reviewNotes`):
  - 中阳 / 中阳县 — 阳 disambiguated to "the sun" (place-name element, closest
    available candidate for the "sunny side of a mountain/river" sense).
  - 主管机关 — 关 disambiguated to "to close; to shut; to turn off" (closest
    candidate to 机关's original "mechanism/pivot" sense).
  - 信阳市 — 信 disambiguated to "trust, confidence" (place-name element, no
    dedicated candidate).
  - 运道 — 道 disambiguated to "road, way" (reinforces the path/way sense of
    fate, not the Daoist or reason sense).

## Content-policy drops this round

None — this batch's 20 words all cleared §3c cleanly. (The 5 recurring
slur/vulgar candidates — 阿三, 小日本, 台巴子, 老黑, 日人民报 — appeared in the
broader `--new --limit=25` planner output but were **not part of this batch** and
were correctly left untouched; other parallel rounds have been dropping-and-
logging them independently per `server/logs/oracle-concerns.md`.)

## Sample authored content

Breakdown elaborations (6, this segment):
- 一血: "'血' stands for a kill; '一血' marks the very first kill of a match."
- 主管机关: "机关 idiomatically means 'agency, organ', from 'mechanism'; 主管 marks it as the governing one."
- 内人: "Wives traditionally kept to the home, so 'inside person' became a humble term for 'wife.'"
- 内传: "'内' marks it as an unofficial, inner account, as opposed to a formal official biography."
- 内内: "Reduplicating 内 ('inner') into playful baby-talk became slang for 'panties.'"
- 运行时: "运行 idiomatically means 'to operate/run' (of a program); adding 时 ('time') gives 'runtime.'"

Classifiers assigned: 内传 → 本 (book/biography); 内内 → 条 (pants/underwear); all
other 18 words → `[]` (no dedicated classifier).

## Round closed

Per standing instruction, this round stops here — no second `--new` batch is
being planned or started in this session.
