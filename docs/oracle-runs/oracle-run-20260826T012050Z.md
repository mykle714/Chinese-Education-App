# Oracle Backfill Run — 2026-08-26T01:20:50Z

Solo, unsharded, autonomous cron round. Custom paths per invocation ARGUMENTS:
resume state → `server/logs/oracle-resume.md`, run notes →
`server/logs/oracle-run-notes.md` (both owned by a parallel worker; a separate
run-notes append was made there, not reproduced here). Stopped cleanly after
one round per explicit instruction (no loop-to-exhaustion this invocation).

## Session budget

- `five_hour`: 11% → 13% (resets 2026-08-26T03:10:00Z)
- `seven_day` / `weekly_all`: 43% → 44% (resets 2026-08-31T01:00:00Z)
- No park (§6a, 95% threshold) or hard-stop (100%) triggered.

## Preflight

- Machine: prod (`amIOnTheProdMachine.md` confirmed).
- `oracle-resume.md`: empty — no parked run to resume.
- `check-manifest-sync.js`: all 24 manifest/script version pairs match — no drift.
- Backup: `server/backups/det-20260826T011735Z-oracle-cron-solo.sql.gz` (17M).

## Scope

zh `--discoverable`: empty (consistent with recent history).

es `--discoverable --limit=50`: 2-script/50-prompt plan —
`spanish/backfill-process-definitions-array` (3 words) and
`spanish/backfill-example-sentences` (47 words). The example-sentences batch
was spot-checked (acabar, acuerdo, agua, ahora, ahí) and reconfirmed as the
known stamp/reality desync (real `exampleSentences` data, no version stamp
for that script) — excluded from this round's scope, not genuine work.

Round scope: the 3-word `spanish/backfill-process-definitions-array` batch
only — granizo, guante, guardar.

## Execution

`spanish/backfill-process-definitions-array` runs pass-1 rank + pass-2
critic in one script invocation; both stages' oracle answers must be present
in `oracle-answers.jsonl` simultaneously for one `apply` call to fully
resolve (a partial answers file causes the script to fall back into export
mode for the missing stage, harmlessly, but requires a second full apply
pass).

| Word | Defs | Result | Critic action |
|---|---|---|---|
| granizo | 2 | unchanged | confirmed |
| guante | 2 | unchanged | confirmed |
| guardar | 7 | updated | confirmed |

Sample authored content — guardar final order: `to keep`, `to save`,
`to put away`, `to save (to store for future use)`, `to save (to write a
file to disk or other storage medium)`, `to order`, `to clean`. No
definitions pruned. `to order` and `to clean` read as possibly-imprecise
Wiktionary-derived glosses for guardar — not obviously attested senses —
but neither met the skill's conservative removal bar (not broken English,
not clearly archaic-and-redundant), so both were kept and ranked last.
Worth a human look.

## Content policy

No drops this round — ordinary vocabulary (hail, glove, to keep/save).

## Verification

- `enrichmentLog->'spanish/backfill-process-definitions-array'` stamped
  `{oracle: true, version: 4}` at 2026-08-26T01:20:50Z for all three words.
- Validator-overwrite guard: 0 rows in `validations` for these three
  entryIds.

## Uncertain answers / cluster review

None this round.

## Resume state

`server/logs/oracle-resume.md` left empty — nothing parked; the batch
completed within this single round.
