# Oracle backfill run — 2026-08-25T21:17:24Z (autonomous cron, unsharded)

## Scope
Single round, `--new` batch (20 zh words), full manifest enrichment → promotion.
Resume/notes files redirected per invocation args to
`server/logs/oracle-resume.md` / `server/logs/oracle-run-notes.md` (a parallel
worker owns the skill's defaults) — neither was written to, since no park
condition was hit and this round completed cleanly start to finish.

## Session budget
- `five_hour.utilization`: 34% (resets 2026-08-25T22:10:00Z)
- `seven_day` (weekly_all): 41% (resets 2026-08-31T01:00:00Z)
- Well under the 95% start-gate and 100% hard-stop; no throttling encountered.

## Backup
`server/backups/det-20260825T191724Z-oracle-cron-20260825.sql.gz`
(dump of `dictionaryentries_zh`, `dictionaryentries_es`, `validations` taken
before any write this round).
Restore: `gunzip -c <path> | docker exec -i cow-postgres-prod psql -U cow_user -d cow_db`

## Batch (20 words, from `oracle-plan.js --new --limit=25`)
红黑名单, 网传, 自传, 西单, 言传, 言传身教, 电传, 风传, 黑名单, 点单,
三反运动, 世运, 亚运, 亚运会, 交运, 国运, 多工运作, 大运, 大运河, 学生运动

## Pipeline run (export → author → apply, per manifest order)
All steps run via `run-prod.sh`, oracle-mode (`BACKFILL_ORACLE=export`/`apply`),
answers appended to `server/logs/oracle-answers.jsonl` (grew 183 → 268 lines
across the run). No hand-written SQL; no `discoverable` set outside
`promote-discoverable.js`.

| Script | Result |
|---|---|
| parts-of-speech / definitions / frequency-score / clusters | completed earlier in the run (pre-compaction) |
| `backfill-cluster-definitions` | 3 self-flagged `⚠ CLUSTER REVIEW`: 世运 (merge-not-split note), 大运 (null-frequency flag), 电传 (split-vs-merge note) |
| `backfill-long-definitions` | 2 length-cap rejections, fixed by shorter re-authored answers on retry |
| `backfill-example-sentences` | 20/20 updated, 0 failed. 2 sentences self-caught grammatical errors on validation (红黑名单, 网传) → regenerated → re-validated → repaired (`Repaired by Opus: 2`). Full 3-sentence-per-word segment tagging completed for all 20, including 2 extra prompt-discovery rounds for the 2 repaired words |
| `backfill-classifier` | 20/20, 0 errors. 9 words got a 量词: 世运/亚运/亚运会/大运→届, 大运河→条, 红黑名单/电传/黑名单→份, 自传→本. 11 got `[]` (non-count-nouns) |
| `backfill-breakdown-senses` | 20/20, 0 failed. 2 self-flagged `⚠ BREAKDOWN SENSE REVIEW`: 交运 (交="to intersect", uncertain), 西单 (单="single/only/sole", place-name etymology note) |
| `backfill-breakdown-elaboration` | 20/20 (2 apply passes; 西单 needed a length-retry — first answer 101 chars vs 50-char hard limit for a 2-char word, rewritten to 36 chars). 6 elaborated (world/sense-opaque): 世运, 亚运, 亚运会, 大运, 红黑名单, 西单. 14 judged straightforward |
| `backfill-tones`, `backfill-numbered-pinyin` | Deterministic, non-AI. Initially misreported "0 pending" without `--stale`; re-run with `--stale` found and fixed 9 (tones) / 20 (numbered-pinyin) stale stamps that were blocking `promote-discoverable.js`'s gate |

## Promotion
`promote-discoverable.js --words=<20-word batch> --apply`:
`Considered: 20 | Promoted: 20 | Already discoverable: 0 | Not ready: 0`

| Word | Difficulty | Classifier |
|---|---|---|
| 三反运动 | 6 | — |
| 世运 | 6 | 届 |
| 亚运 | 5 | 届 |
| 亚运会 | 5 | 届 |
| 交运 | 5 | — |
| 国运 | 5 | — |
| 多工运作 | 6 | — |
| 大运 | 5 | 届 |
| 大运河 | 5 | 条 |
| 学生运动 | 5 | — |
| 红黑名单 | 5 | 份 |
| 网传 | 4 | — |
| 自传 | 4 | 本 |
| 西单 | 6 | — |
| 言传 | 5 | — |
| 言传身教 | 6 | — |
| 电传 | 6 | 份 |
| 风传 | 6 | — |
| 黑名单 | 4 | 份 |
| 点单 | 3 | — |

## Verification (§5)
Direct SQL against `dictionaryentries_zh` for all 20 words confirmed:
`discoverable=true`, `partsOfSpeech`/`longDefinition`/`exampleSentences`/
`frequencyScore`/`breakdown` all non-null, `classifier` arrays matching the
promotion output above, and `difficulty` within 1–6 for every row. None of
these 20 words were previously discoverable, so no `validations` row could
exist for them prior to this run — the validator-protection guard was not
exercised this round.

## Content policy (§3c)
No new drops this round. Cumulative drops across all rounds (unchanged, in
`server/logs/oracle-concerns.md`): 阿三, 小日本, 台巴子, 老黑, 日人民报.

## Notes / uncertain items for human review
- Cluster review flags: 世运 (merge-not-split judgment call), 大运 (frequency
  score came back null on one cluster), 电传 (split-vs-merge judgment call).
- Breakdown-sense review flags: 交运 (交's in-word sense picked as "to
  intersect" — uncertain), 西单 (单's sense picked as "single/only/sole" with
  a place-name-etymology caveat — 西单 is a well-known Beijing place name and
  the breakdown is etymological rather than compositional).
- 西单's elaboration needed a length-retry; final text: "单 recalls a single
  arch that once stood here." (36 chars).

## Outcome
20/20 words fully enriched and promoted to `discoverable = TRUE`. Round
completed cleanly in one pass — no park, no second round started, per the
invocation's explicit instruction.
