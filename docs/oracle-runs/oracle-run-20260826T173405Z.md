# Oracle backfill run — 2026-08-26T17:34:05Z

Solo, unsharded cron round. Round notes: `server/logs/oracle-run-notes.md`
(round section "20-word zh batch (清关 cluster)" precedes the immediately-preceding
report; this round is the next one, unlabeled in-line but appended directly after
the "backfill-cluster-definitions.js — COMPLETE (20/20 words)" entry).

## Scope

`dictionaryentries_zh`, 20 words newly shipped this round:
关卡, 关口, 关城, 关外, 关子, 关山, 关文, 关东, 关格, 关机, 关白, 关西, 关门, 关头,
阳, 阳世, 阳信, 阳信县, 阳光, 阳城.

Backup: `server/backups/det-20260826T161718Z-oracle-cron-20260826.sql.gz`.

## Manifest steps run

Full zh manifest run in dependency order this session: parts-of-speech, word-forms,
hsk-level, frequency-score, process-definitions-array, cluster-definitions,
long-definitions, longdef-citations, example-sentences, classifier,
breakdown-senses, breakdown-elaboration, tones, numbered-pinyin — see round notes for
the per-script trace and counts.

Two deterministic (non-LLM) steps needed a `--stale` re-run: `backfill-tones.js` and
`backfill-numbered-pinyin.js` both reported "0 entries needing backfill" on a plain
run because the columns already held correct data from an earlier unstamped pass;
`--words=<batch> --stale` stamped all 20 rows for each with zero data changes
(verified via direct read on 关卡: `tone=13`, `numberedPinyin='guan1 qia3'`, unchanged
before/after).

`backfill-breakdown-elaboration.js` (19/19 multi-char words, excludes single-char 阳):
`Processed: 19, Elaborated: 7, Straightforward: 12, Undecided: 0, Failed: 0`. Elaborated:
关子, 关文, 关东, 关白, 关西, 关头, 阳世 — all idiomatic/figurative compounds or
classical-cosmology terms whose literal character breakdown doesn't obviously convey
the word meaning (elaboration text in round notes).

`backfill-breakdown-senses.js` (19/19): 0 self-flagged review notes this batch — every
component character had an unambiguous best match among its candidate senses.

## Promotion

`promote-discoverable.js --words=<batch> --apply`: `Considered: 20 | Promoted: 20 |
Already discoverable: 0 | Not ready: 0`.

## Verification (skill §5)

All 20 rows: `partsOfSpeech`/`longDefinition`/`exampleSentences`/`frequencyScore`/
`definitionClusters` non-null, `discoverable = TRUE`, `difficulty` populated in range
[2,6]. 0 rows in the discoverable-without-valid-difficulty sweep. `validations` table
(joined on `"entryId"`): 0 rows for any of the 20 ids — no validator-approved/flagged
field existed to protect or risk overwriting.

## Content policy

No drops this round. 关卡/关口/... and 阳/阳世/... are all ordinary vocabulary
(checkpoint/pass compounds, place names, yin-yang cosmology terms). The 5 recurring
poison-list candidates (阿三, 小日本, 老黑, 台巴子, 日人民报) were not part of this
batch — they remain non-discoverable from earlier rounds' drops; see
`server/logs/oracle-concerns.md` for the full drop history.

## Status

Round complete. Per the standing cron instruction (solo, unsharded, stop after one
round), no second batch was started this invocation. `server/logs/oracle-resume.md`
was not written to — no parked state, nothing to hand off.
