# Oracle Backfill Run — 2026-08-25T13:17:27Z (solo cron, unsharded)

Autonomous single-round invocation. ARGUMENTS explicitly overrode the skill's default
loop-to-exhaustion behavior: run one round, redirect resume/notes state to
`server/logs/oracle-resume.md` / `server/logs/oracle-run-notes.md` (owned by a parallel
worker), and stop cleanly without a second round. No user check-ins occurred at any
point, per skill §1.5.

## Scope

Continuation of a prior (different-session) round's leftover work, discovered via
`oracle-plan.js --lang=es --discoverable --limit=50`:

- `spanish/backfill-process-definitions-array` (v4) — 27 words
- `spanish/backfill-example-sentences` (v1) — 23 words

`oracle-plan.js --lang=zh --discoverable` had nothing pending; zh was not touched.
`--new` scope (shipping brand-new words) was never reached — the es discoverable
backlog alone filled the round.

## Backup

`server/backups/det-20260825T131727Z-oracle-cron-solo.sql.gz` (17M), via
`server/scripts/backfill/backup-det.sh oracle-cron-solo`. Restore:
`gunzip -c <path> | docker exec -i cow-postgres-prod psql -U cow_user -d cow_db`.

## `spanish/backfill-process-definitions-array` — 27 words, all converged

Words: carta, cazar, cazo, celos, celoso, cepillo, cerebro, chuleta, cielo,
científico, colador, colina, collar, colocar, columna, comida, comprar, concierto,
conducir, conductor, confiar, congelado, construir, contraseña, coro, correo, corteza.

This script is a genuine **three-stage chain** (documented as "Pass 1 + critic" in its
own startup banner, but a lead-gloss-length overflow adds a third stage):

1. **Pass 1** — junior RANK (reorder + prune an existing definition array) or SPLIT
   (decide whether a comma-joined single definition is actually multiple senses).
   23 RANK + 4 SPLIT prompts.
2. **Critic** — every pass-1 result (including an empty SPLIT, which defers straight
   to a fresh RANK) is re-reviewed by a second, differently-worded prompt returning
   `{action, finalOrder, reason}`. 27 more prompts.
3. **Short-gloss** — only if the critic-confirmed lead definition exceeds the card's
   20-character headline budget. Triggered once, for `corteza` (`"crust (of food or
   geologically)"` → prepended short gloss `"crust"`).

Total: 55 authored answers across 3 stages, 0 failures on final convergence.

**Finding worth flagging**: the prior round's notes raised a concern that a chained
script issuing "a new, distinct oracle promptId" per retry might indicate
`oraclePromptId()` isn't purely a hash of `(model, system, messages)`. Direct
comparison of the pass-1 vs. critic prompt for the same word (`carta`) shows the
`messages` genuinely differ — the critic prompt appends an "Original input order" /
"Junior's proposed order" block absent from pass 1 — so the new hash is correct,
expected behavior, not a determinism bug. Recommend treating that prior concern as
resolved.

Sample of authored judgment calls:

- `columna`: junior (pass 1, me) proposed `[a column, file, a circular support]`;
  critic (me, second pass) refined it back to `[a column, a circular support, file]`
  — the architectural-support sense is core/everyday, the "single-file line" sense is
  a narrower idiomatic extension and shouldn't outrank it. This is exactly the
  self-correction the two-stage design is for.
- `correo`: `[mail, email, post office]` → junior reordered to lead with `email`;
  critic marked `low_confidence` (both are defensible modern leads) and kept `email`
  first as the best guess, per the ranking principle that everyday/tech modern usage
  beats the etymological-order default.
- `corteza`: split `"rind, peel (of a fruit)"` → `["rind", "peel (of a fruit)"]` in
  pass 1; critic then reordered the full 4-item list to lead with `"crust (of food or
  geologically)"`; that lead was then shortened to `"crust"` for the headline slot.
- `cazo`, `collar`: SPLIT stage found no split needed (empty `splits: []`); a fresh
  RANK prompt on the original 2-def list followed, both confirmed unchanged by the
  critic.

## `spanish/backfill-example-sentences` — 23 words, confirmed genuine no-op

Words: acabar, acuerdo, agua, ahora, ahí, amigo, antes, aquí, así, ayudar, año, beber,
bien, bueno, buscar, casa, chico, ciudad, claro, comer, conocer, cosa, creer.

Export phase found **0 entries needing example sentences** — the script's own
`doneGate` correctly saw all 23 already had populated `exampleSentences` (3–12 per
word) and skipped them. Verified directly against `dictionaryentries_es`. This is the
same **stamp/reality desync bug class** already documented in a prior round for this
exact script: none of these 23 rows carry an `"spanish/backfill-example-sentences"`
key in `enrichmentLog` at all, so the version-aware planner reads them as never-run and
lists them every round, while nothing is actually missing. Not fixed this round (would
require changing the script's skip path to still call `stampEntries`); flagged again
for whoever eventually does a stamp-bookkeeping cleanup pass.

## Guardrail evidence

- `enrichmentLog->'spanish/backfill-process-definitions-array'` on all 27 words:
  `version: 4, oracle: true`, fresh `ranAt` (2026-08-25T13:25:xx UTC).
- Validator-overwrite guard (`validations` joined to these 27 det ids): **0 rows** —
  no validator activity on this batch, nothing at risk of being clobbered.
- No slur or explicit-sexual content encountered — all 27 words are ordinary
  everyday Spanish vocabulary. `server/logs/oracle-concerns.md` untouched this round
  (its existing entries are all from an earlier zh round).

## Stop

Per this invocation's ARGUMENTS, the round ends here. A re-run of `oracle-plan.js
--lang=es --discoverable` afterward shows a fresh 21-word `process-definitions-array`
batch (`cruel, cuchillo, cuello, culpa, césped, cómo, delito, derretir, …`) has
surfaced — expected, the manifest backlog simply advanced — but it is **not** started;
starting it would be a second round. `server/logs/oracle-resume.md` stays empty (no
in-flight state to hand off). Full narrative notes appended to
`server/logs/oracle-run-notes.md`.
