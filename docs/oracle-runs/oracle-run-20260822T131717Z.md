# Oracle run — 2026-08-22T13:17Z (autonomous cron round, solo/unsharded)

Single round, as instructed by the invocation ("stop cleanly at the end of one round").
Scope: `--discoverable` refresh on already-shipped zh rows. No `--new` round, no promotion.

## Session budget

| Window | five_hour before | five_hour after | seven_day | Rounds |
|---|---|---|---|---|
| 2026-08-22 13:17Z → 13:40Z | 0.0 % (`resets_at: null`) | 0.0 % | 100 % (resets 2026-08-24T01:00Z) | 1 |

**Which guardrail ended the run:** none — the run ended on the invocation's explicit
one-round instruction, not on a §6 guardrail.

**Flat-usage check (§6): flat reading recorded, halt NOT warranted.** `five_hour`
utilization read 0.0 before and after a round that wrote 10 rows. Reasoning:

- The failure this check exists to catch is answers arriving from outside the session.
  That is ruled out directly: `ANTHROPIC_API_KEY` is **unset** in the environment and
  absent from both repo-root `.env` and `server/.env.docker`; every one of the 54
  prompts in `server/logs/oracle-prompts.jsonl` has a matching hand-authored line in
  `server/logs/oracle-answers.jsonl`, and each apply pass advanced **only** as far as
  the answers I had written — the chain converged one link at a time under my hand,
  which no bypassing call path could produce.
- The account is at `seven_day` 100 % with **no five-hour window open** (`resets_at:
  null`). Under that state the five-hour counter reads 0 regardless of work done, so
  it carries no signal this round.
- The round was small in oracle terms: 10 words, one script, 54 authored answers —
  comfortably inside a single integer percentage point at the documented ~1 point per
  25–30 words.

⚠️ **Note for the next operator:** `seven_day` was already at 100 % at preflight. This
round was authored anyway (requests were being served), but the weekly pool is spent
until **2026-08-24T01:00Z**. Rounds scheduled before then may be refused mid-chain.

## Backup

- `server/backups/det-20260822T131726Z-oracle-cron.sql.gz` (17 M)
- Restore: `gunzip -c server/backups/det-20260822T131726Z-oracle-cron.sql.gz | docker exec -i cow-postgres-prod psql -U cow_user -d cow_db`

Manifest sync (`check-manifest-sync.js`) verified clean before the round — all 17 zh+es
entries matched their `SCRIPT_VERSION`.

## Words

**Newly discoverable: none.** This was a refresh round; all 10 rows were already shipped.

**Refreshed (reason: `definitionClusters` null — the only step the planner reported pending):**

| Word | id | Reading | Clusters written | difficulty |
|---|---|---|---|---|
| 特点 | 71316 | te4 dian3 | 1 | 4 |
| 独立 | 72006 | du2 li4 | 1 | 4 |
| 独立自主 | 72012 | du2 li4 zi4 zhu3 | 1 | 5 |
| 现在 | 72716 | xian4 zai4 | 1 | 1 |
| 理 | 72828 | li3 | 6 | 4 |
| 理事 | 72830 | li3 shi4 | 2 | 5 |
| 理光 | 72833 | li3 guang1 | 1 | 6 |
| 理化 | 72837 | li3 hua4 | 2 | 5 |
| 理学 | 72842 | li3 xue2 | 2 | 6 |
| 理学家 | 72844 | li3 xue2 jia1 | 1 | 6 |

## Per script

**`chinese/backfill-cluster-definitions` (v6)** — the only script the planner named.

| | |
|---|---|
| Prompts exported | 54 (across 17 apply passes; the script chains cluster → per-cluster rank → critic → per-cluster frequency, one link per pass) |
| Answers authored | 54 |
| Rows updated | 10 |
| Rows rejected by the validator | 0 on the final pass |

**One answer was rejected before it ever reached the validator, and how it was corrected:**
my first authored answer for 理学 (`61b2604ac7c990da`) mangled one character while copying
the long Neo-Confucian gloss — `程顥|程颢` became `程顥|程顓`. Rule 1 of the clustering
prompt requires glosses copied character-for-character, so this would have failed the
partition check. I caught it with a pre-apply harness that re-parses every prompt's
`Definitions:` block and asserts the authored partition is set-equal to the input; it
reported the mismatch, and the gloss was repaired from the source string rather than
retyped. **Recommendation: that check is worth doing every round** — a hand-copied
long gloss is exactly where character drift hides, and the drift is invisible by eye.

The `FAILED: oracle-export: captured prompt` lines in passes 1–16 are the documented
chain-advance behaviour (a newly-reachable link being captured), not validation failures.

## A1.5 pronunciation decisions

No reading was changed. Every cluster took the entry's primary reading; no heteronym was
asserted. The one place a heteronym could plausibly have been claimed is 理 — it has none,
and all six clusters were written `li3`.

## Cluster review flags (verbatim)

```
⚠ CLUSTER REVIEW 独立 (id=72006): Kept 'to stand alone' with the independence senses as one origin (physical standing-alone → political/personal independence). Flag in case you'd split the literal posture sense of 独立 out.
⚠ CLUSTER REVIEW 理光 (id=72833): Proper noun (brand name); tagged 'noun' as there is no proper-noun tag in use.
⚠ CLUSTER REVIEW 理学 (id=72842): Ordering is uncertain: neither sense is everyday. Put 'science' first (transparent, survives in 理学院/理学士) ahead of the philosophy-of-history sense; the Neo-Confucian sense is arguably the primary dictionary sense and could be ranked first.
⚠ CLUSTER REVIEW 理 (id=72828): Register split (rule 4): everyday 'reason/logic/truth' separated from the Neo-Confucian/philosophical 'intrinsic order / inner essence', which share one origin but sit in very different registers. Merge them if you'd rather score them together.
⚠ CLUSTER REVIEW 理 (id=72828): 'to pay attention to' (不理他) split from 'to manage/handle'; they are arguably one attend-to origin. Flag.
⚠ CLUSTER REVIEW 理 (id=72828): 'texture / grain (of wood)' is the etymological source sense and is now largely bound (纹理, 肌理); kept as its own cluster.
```

## Guardrail evidence

- Rows excluded by `validatedClause`: the planner reported **no** `🛡 validator-protected`
  rows in this batch.
- No approved/flagged field was written — the §5 query returns 0 rows:
  ```
  SELECT val.field, val.action, d.word1
  FROM validations val JOIN dictionaryentries_zh d ON d.id = val."entryId"
  WHERE val.action IN ('approve','flag') AND d."enrichmentLog" IS NOT NULL
    AND (d."enrichmentLog" #>> '{chinese/backfill-cluster-definitions,ranAt}')::timestamptz
        > '2026-08-22T13:17:00Z';   -- (0 rows)
  ```
- `discoverable_without_level` = **0**. No row was flagged outside `promote-discoverable.js`.
- Post-round re-plan of the batch: **0 prompts across 0 scripts** — the batch is complete,
  and clustering unblocked no further step (all downstream steps were already stamped).
- All 10 rows carry non-null `partsOfSpeech`, `longDefinition`, `exampleSentences`,
  `frequencyScore`, `definitionClusters`, and a difficulty in 1–6.

## Sample of the content actually authored

**理 (id=72828) — the hard row, 15 glosses → 6 clusters:**

| Sense | pos | Glosses | freq |
|---|---|---|---|
| reason / principle | noun | reason; logic; truth | 2 |
| to take notice of | verb | to pay attention to | 4 |
| to manage / put in order | verb | to manage; to handle; to put in order; to tidy up; to run (affairs) | 2 |
| the natural sciences | noun | natural science (esp. physics); science | 2 |
| texture / grain | noun | texture; grain (of wood) | 1 |
| underlying principle (philosophical) | noun | intrinsic order; inner essence | 1 |

The one deliberate re-rank I made as critic: for 理光 I moved "Ricoh, Japanese imaging
and electronics company" ahead of the bare "Ricoh" ("the fuller gloss identifies the
referent for a learner who does not recognise the brand name alone"), and for 理学 I
moved "School of Principle" ahead of the encyclopedic Cheng-Zhu gloss.

**Long definitions already on these rows** (unchanged by this round; quoted so the
clusters above can be judged against them):

> **特点** — *distinguishing feature (noun)*: "A quality that sets a thing, a place, or a
> person apart from others of the same kind, and so is what you would name if asked what
> makes it what it is."
>
> **理** — *to take notice of someone (verb)*: "To acknowledge a person who wants your
> attention. It appears most often in the negative, describing someone being ignored outright."
>
> **独立自主** — *(verb)*: "To run one's own affairs and settle one's own course, taking
> direction from no one else."

## Uncertain / worth a human eye

1. **理学 cluster order** is a genuine coin-flip (flagged above). I led with "science";
   a Chinese-lexicography reading would probably lead with the Neo-Confucian sense.
2. **理 register split** (`reason/logic/truth` vs `intrinsic order/inner essence`) is my
   reading of rule 4. They are one origin; if you would rather they shared a frequency
   score, merge them.
3. **理 "to pay attention to" scored 4** — high for a bare morpheme, justified because
   不理他 / 别理我 are genuinely everyday. The other 理 senses are bound in modern speech
   and scored 1–2 accordingly; if you think a bound morpheme should not score 4 at all,
   this is the row to revisit.
4. **理化 = 2** assumes school-subject shorthand is spoken by students. Could be a 1.
5. **独立 kept "to stand alone" merged** with the independence senses (flagged above).

## Findings for the backlog (recorded, not acted on)

- **The per-cluster frequency prompt truncates its gloss list to 4.** For 理's
  "to manage / put in order" cluster the prompt showed `to manage; to handle; to put in
  order; to tidy up` and omitted `to run (affairs)`, which *is* present in the written
  row. Display-only for the stored data, but the scorer is judging a truncated sense on
  any cluster wider than four glosses.
- **The apply chain advances exactly one link per pass, and each pass re-runs the whole
  `--words` batch.** 理 alone needed 17 passes. Scoping later passes to the laggard word
  (`--words=理`) is much cheaper than re-running all ten. Worth considering whether the
  script could capture all reachable prompts for a row in one unwind.
- **`oracle-prompts.jsonl` is append-only across runs**; the skill's `rm -f` only covers
  the first export. Anything reading it mid-chain must dedupe by `promptId`.

These are also appended to `server/logs/oracle-run-notes.md` per the invocation's paths.
