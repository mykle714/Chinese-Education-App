# Oracle backfill run — 2026-08-25T06:17:24Z (solo, unsharded, cron)

## Session budget

| Window | Utilization at end of round | resets_at |
|---|---|---|
| five_hour | 87% | 2026-08-25T07:09:59Z |
| seven_day (weekly_all) | 31% | 2026-08-31T00:59:59Z |

No cap breach. Round completed inside one five-hour window.

## Backup

`server/backups/det-20260825T061724Z-cron-solo-20260825T061724Z.sql.gz`
(taken before this round's writes; covers `dictionaryentries_zh`,
`dictionaryentries_es`, `validations`).

Restore:
```bash
gunzip -c server/backups/det-20260825T061724Z-cron-solo-20260825T061724Z.sql.gz | \
  server/scripts/backfill/run-prod.sh-equivalent psql restore   # see backup-det.sh header for exact invocation
```

## Words newly discoverable (20)

皮山, 皮山县, 皮星, 皮尔, 皮特拉克, 皮球, 皮重, 皮面, 皮黄, 眼皮, 眼皮子, 开发人员, 管理员,
网格员, 网管员, 行员, 西皮, 车皮, 重眼皮, 重眼皮儿

All 20 were `oracle-plan.js --new` candidates carried into this round already
mid-manifest (classifier / breakdown-senses / breakdown-elaboration were the only
oracle steps outstanding at round start).

## Content-policy drops (§3c)

5 words dropped from the same `--new` candidate pool, not enriched, not promoted:

- 阿三 — slur (derogatory epithet for Indians)
- 小日本 — slur (derogatory epithet for Japanese people)
- 台巴子 — slur (derogatory epithet for Taiwanese people)
- 老黑 — slur (derogatory epithet for Black people)
- 日人民报 — explicit-sexual (vulgar pun on 人民日报)

Logged to `server/logs/oracle-concerns.md`. This is the **third consecutive round**
this exact slur cluster has resurfaced from `oracle-plan.js --new` — the planner has
no drop-list/exclusion mechanism, so these 5 words will keep reappearing at the head
of every future `--new` batch until one is added upstream. Flagging for review rather
than fixing unilaterally, since it changes the planner's selection contract.

## Per-script counts

| Script | Prompts | Answered | Updated | Rejected | Notes |
|---|---|---|---|---|---|
| backfill-classifier (v2) | 20 | 20 | 20 | 0 | single-stage, no chaining |
| backfill-breakdown-senses (v1) | 20 | 20 | 20 | 0 | single-stage; 6 self-flagged review notes (transliterations/opera-tune abbreviations) |
| backfill-breakdown-elaboration (v1) | 20 + 2 | 22 | 20 | 0 | 2/20 words (皮黄, 西皮) needed a second pass — first elaboration exceeded the length cap, script re-prompted with a "too long, rewrite" retry keyed on a new promptId; answered within 50-char cap on retry |
| backfill-tones (v1) | — | — | 12 | 0 | deterministic, no AI call; oracle export was a no-op (0 rows matched — already-stamped rows just needed `--stale` to re-derive); ran directly outside oracle mode |
| backfill-numbered-pinyin (v1) | — | — | 20 | 0 | deterministic, no AI call; same doneGate-vs-promotion-gate gap as tones; ran directly with `--stale` |

Total oracle prompts this round: 42 (20 classifier + 20 breakdown-senses + 22
breakdown-elaboration). Total deterministic rows updated: 32 (12 tones + 20
numbered-pinyin).

## Repeat finding: tones/numbered-pinyin doneGate vs. promotion gate

Same gap seen in the prior round (2026-08-25T04:17:23Z): `backfill-tones.js` and
`backfill-numbered-pinyin.js` gate their own "needs backfill" query on the column
being `NULL`, but `promote-discoverable.js` gates on the `enrichmentLog` version
stamp. A row can have the column populated (from an earlier, unstamped run) yet still
read as "not ready" by the promoter. First dry-run of `promote-discoverable.js` this
round blocked all 20 rows on `backfill-tones`/`backfill-numbered-pinyin`; running each
script's own detection found only 12 (tones) and 20 (numbered-pinyin, i.e. all)
actually missing the stamp — resolved with `--stale` (tones) and a plain re-run
(numbered-pinyin), no oracle authoring, no hand-written SQL. Second dry-run showed all
20 clear. This is now a two-round-repeated pattern; worth a proper fix (stamp-backfill
migration or aligning the two doneGates) rather than re-discovering it every round —
noted here rather than fixed, since it's out of scope for an oracle-answering round.

## Guardrail evidence

```sql
SELECT count(*) AS discoverable_without_level FROM dictionaryentries_zh
WHERE language='zh' AND discoverable AND (difficulty IS NULL OR difficulty NOT BETWEEN 1 AND 6);
-- 0

SELECT count(*) FROM validations v JOIN dictionaryentries_zh d ON d.id = v."entryId"
WHERE d.word1 IN (<20-word batch>) AND d.language='zh';
-- 0 (no validator-approved fields at risk of overwrite in this batch)
```

`/mark-discoverable` §A4 completeness check — all 20 rows: `partsOfSpeech`,
`longDefinition`, `exampleSentences`, `frequencyScore`, `breakdown`, `classifier` all
non-null, `difficulty` in [1,6]. Passed clean on first check after the tones/pinyin fix.

## Sample authored content

`backfill-breakdown-elaboration` — 6 of 20 words got a non-null elaboration, all
transliterations or opera-tune abbreviations where stacking the character glosses
would mislead a learner:

- 皮山 / 皮山县 — "A Xinjiang county name; 皮 and 山 are used for their sounds, not
  their literal meanings." (+ 县 = county for the 3-char form)
- 皮尔 — "A US city name (Pierre, SD); both syllables are chosen for their sound, not
  their literal meanings."
- 皮特拉克 — "A transliteration of the Italian name Petrarch; none of the four
  syllables carry their literal meanings here."
- 皮黄 (retry, 41 chars) — "Short for 西皮, an opera tune-type; syllables are
  non-literal."
- 西皮 (retry, 38 chars) — "An opera tune-type name; both syllables are non-literal
  here."

`backfill-classifier` — sample of measure-word judgments: 皮球 (rubber ball) → 个;
车皮 (freight car) → 节; 重眼皮/重眼皮儿 (double eyelid) → 双; 管理员/网格员/网管员/
行员/开发人员 (person-role nouns) → 位, 名; place names/abstract/mass nouns (皮山,
皮重, 皮面, 皮黄, 西皮, etc.) → `[]`.

## Uncertain items

The 6 breakdown-senses review-flagged words above (皮山, 皮山县, 皮尔, 皮特拉克, 皮黄,
西皮) — all self-flagged by the model as "no listed character sense is truly
semantically active" (transliterations and opera-tune abbreviations). Worth a human
skim of their breakdown display, though the flag itself was already the correct
signal and no bad data resulted (each fell back to the character's literal/primary
sense plus an elaboration explaining the sound-only usage).

## Cluster review flags

None (`backfill-cluster-definitions` was not part of this round's outstanding-step
list — it completed for this batch in an earlier round before compaction).

## Why the round stopped here

Per this invocation's arguments ("Stop cleanly at the end of one round; do not start
a second"), the round ends here. `server/logs/oracle-resume.md` is left empty — no
in-flight state to hand off; the batch fully completed and verified within this round.
