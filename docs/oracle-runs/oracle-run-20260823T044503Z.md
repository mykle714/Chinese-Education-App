# Oracle backfill run — 2026-08-23T04:45:03Z (round 530, unsharded solo cron round)

**Machine:** PROD. **Language:** zh. **Rounds completed:** 1 (stop-after-one-round instruction honored).

## 1. Backup
`server/backups/det-20260823T041716Z-r530.sql.gz` (17M) — `dictionaryentries_zh`, `dictionaryentries_es`, `validations`.

## 2. Scope
`oracle-plan.js --discoverable` returned nothing pending (12th consecutive round), so the round
fell through to §3b `--new`. 60 planner candidates were inspected in the DB; ~48 were dropped as
explicit senses, slurs, place-name stubs or brand names, leaving a clean batch of 12.

**Batch:** 流行音乐, 管乐器, 音乐学, 民乐, 乐音, 乐道, 同乐会, 和乐, 鱼水, 鱼花, 鱼龙, 老头乐

## 3. Scripts run (planner order)
| Script | Rows | Result |
|---|---|---|
| backfill-tones | 5 | needed `--stale` (values present, no stamp) |
| backfill-numbered-pinyin | 12 | needed `--stale` |
| backfill-parts-of-speech → frequency-score → cluster-definitions → process-definitions-array → word-forms → long-definition → difficulty | 12 each | Errors 0 |
| backfill-example-sentences | 12 | Errors 0; multi-wave (generate → native review → repair → re-review → segment labelling) |
| backfill-classifier | 10 (nounPos) | 6 with classifier, 4 without |
| backfill-breakdown-senses | 12 | Errors 0, 1 review flag |
| backfill-breakdown-elaboration | 12 | 10 elaborated, 2 straightforward |

Planner re-check after the last step: **0 prompts across 0 scripts.**

## 4. Promotion
`promote-discoverable.js --words=<batch>` — dry run 12/12 ready, then `--apply`: **Promoted 12, Not ready 0.**

## 5. Verification
- All 12 rows: `partsOfSpeech`, `longDefinition`, `exampleSentences`, `frequencyScore`, `breakdown` non-null; `difficulty` 4–6; `discoverable = t`.
- `discoverable_without_level` = **0**.
- Validator-guard query: **0 rows** — no approved/flagged field moved.

## 6. Findings
1. **`--stale` needed again for tones / numbered-pinyin** (5 and 12 rows). Fourth recurrence
   (rounds 527–530): the values exist but carry no `enrichmentLog` stamp, so the planner lists
   them while the script reports "0 needing" until `--stale` is passed.
2. **`乐` has no `yuè` "music" sense in its character entry.** `backfill-breakdown-senses` offered
   only "happy / joyful" and "to laugh", so 乐音 / 民乐 / 流行音乐 / 管乐器 / 音乐学 were all tagged
   with a sense that is wrong in context (each carries a `reviewNote`, and the elaborations state
   the yuè reading explicitly as a workaround). This is a **data gap in the 乐 det row's
   `definitionClusters`**, not a pipeline bug, and it will keep mis-tagging every 音乐-family word
   until that row gains the music sense.
3. **Segmentation quirks:** `得很` emitted as one segment; `老人们` split as 老 + 人们 (should be 老人 + 们);
   `海里` kept as one segment where the sentence means 海 + 里.
4. **Self-flagged reviews:** ⚠ CLUSTER REVIEW on 乐道 (split across the two senses of 道), 管乐器
   (woodwind-as-subtype), 老头乐 (elderly-products extension); ⚠ BREAKDOWN SENSE REVIEW on 鱼花
   (花 = specks of spawn, absent from the candidate list).
5. **Native review rejected 2 of 36 of the oracle's own sentences** — 同乐会 #2 (`translation_mismatch`)
   and 乐道 #2 (`unnatural_phrasing`); both repaired and accepted on re-review.
6. `five_hour.utilization` read 100.0% at run start while requests kept succeeding — the usage
   endpoint and the actual limiter disagreed.

## 7. Oracle-mode caveat
Every answer in this round was authored and reviewed by the same model that later graded it in the
native-review and judge stages. The validators were not weakened, but self-review is inherently
weaker than independent review.
