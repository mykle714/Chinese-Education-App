# Oracle backfill run — 2026-08-26T23:30:00Z (cron-solo-20260826b, continuation)

Autonomous, unsharded solo round. Continuation of a parked apply pass for
`spanish/backfill-process-definitions-array` — no new batch was planned or started
this round per the invocation's "stop cleanly at the end of one round" instruction.

## Backup

`server/backups/det-20260826T231733Z-cron-solo-20260826b.sql.gz`

## Scope

- Lang: es
- Scope: `--discoverable` (heal already-shipped rows)
- Script: `spanish/backfill-process-definitions-array` (manifest v4, `when: multiDef`)
- Words (25, 24 actually produced prompts): masticar, matrimonio, mecánico, medir,
  mensaje, mentiroso, merienda, mezclar, mirada, molestar, montaña, mosca, mostrador,
  muñeca, músculo, nacimiento, nadar, naranja, necesitar, negar, niño, nube, obligar,
  ocurrir, odio. (`necesitar` never produced a prompt — already converged for this
  script; not investigated further.)

## What happened

The previous session's apply attempt on this batch appeared to fail all 24 words with
`FAILED: oracle-export: captured prompt <id>`. This is **not a bug** — `oracleCreate()`
(`server/scripts/backfill/run-log.js`) captures-and-unwinds on any request hash not yet
answered in `oracle-answers.jsonl`, which is the documented behavior for scripts with
multi-call chains (comma-split check → pass-1 ranking → pass-2 critic, plus a
short-gloss step when a pruned/reordered list changes the lead definition's length,
and an occasional re-verification pass after a write). Each apply pass answers exactly
the prompts the row is blocked on and advances one link.

Convergence took 6 export/apply round-trips total. Words with a nontrivial chain:
mentiroso, molestar, muñeca, nacimiento, naranja, obligar — each needed 2-4 rounds as
comma-split → ranking → critic → (short-gloss for muñeca/nacimiento) → re-ranking →
re-critic played out. The rest converged in round 1 (pure ranking, no split needed) or
round 2 (critic confirmed the round-1 ranking outright).

Final apply pass: `Total processed: 24, Updated: 0, Unchanged: 24, Failed/invalid: 0`.

## Sample authored content

- masticar → `["to chew", "to think (about an idea)"]` (no split, no removal)
- mirada → comma-split `"look, glance"` → `["look", "glance"]`, then ranked
  `["look", "glance"]`
- muñeca → ranked `["doll", "female equivalent of \"muñeco\" (\"doll\")", "wrist"]`
  after the lead def ("female equivalent of...") triggered a ≤20-char short-gloss
  request, answered `"doll"`
- nacimiento → comma-split `"birth, beginning, start"` → 3 glosses and
  `"crib, nativity scene"` → 2 glosses, then ranked with `"nativity scene"` ahead of
  `"crib"`, then re-ranked with the short gloss `"birth"` prepended after its long
  lead definition triggered the same 20-char check
- obligar → comma-split all three synonym runs (`"to force, to compel, to make..."`,
  `"to oblige, to obligate, to bind"`, `"to be bound, to be obliged, to be
  obligated"`) into 9 pieces, then ranked all 11 resulting glosses
- naranja → comma-split check offered **twice** across the round on
  `"a member or supporter of Ciudadanos, a Spanish political party"` — declined both
  times as the skill's own worked DO-NOT-SPLIT example (prose continuation, not a
  synonym run)
- mentiroso → pruned `"having lies"` (broken/redundant English already covered by
  `"having the qualities of a liar"`)

## Judgment calls to flag

- **mentiroso**: removal of "having lies" — broken English duplicate, not a
  borderline call.
- **muñeca**: ranked "doll" sense ahead of "wrist" — a frequency judgment, not
  dictated by any worked example in the skill.
- **nube**: did NOT split `"multitude, or crowd (of people)"` — the "or"-joined
  pattern isn't covered by any worked split/no-split example; left alone per
  "when in doubt, leave the gloss alone."
- **naranja**: confirmed as the skill's own DO-NOT-SPLIT worked example.

## Verification

```sql
SELECT word1, jsonb_array_length(definitions) AS n_defs, definitions->0 AS lead_def
FROM dictionaryentries_es WHERE language='es' AND word1 IN (<25 words>);
```
All 25 rows have a sane, non-parenthetical, reasonably short lead definition.

```sql
SELECT d.word1, v.field, v.action FROM validations v
JOIN dictionaryentries_es d ON d.id = v."entryId" AND d.language = v.language
WHERE d.language='es' AND d.word1 IN (<25 words>);
```
0 rows — no validator-approved/flagged field existed on any of these words, so nothing
protected was at risk.

## Recurring known issue (not investigated, per standing decision)

`oracle-plan.js --lang=es --discoverable --limit=50`, re-run after this batch, again
surfaces `backfill-example-sentences` with the same ~33-word "phantom batch" seen in
prior rounds (hecho, hijo, hola, hombre, hora, ir, leche, libro, …). This is the known
planner/script staleness-model mismatch documented in earlier run notes; per standing
decision this run does not attempt a fix, only re-confirms it recurred.

## Next round's queue (not started this round)

Planner shows, immediately available for `--lang=es --discoverable`:
- `spanish/backfill-process-definitions-array` (v4): 20 fresh words (minuto, moneda,
  morir, mosquito, mostrar, mujer, multiplicar, muro, …+12)
- `backfill-example-sentences` (v1): the recurring 33-word phantom batch above

## Stop condition

Per this invocation's ARGUMENTS ("stop cleanly at the end of one round; do not start a
second round"), the round ends here. No promotion step was needed (all touched rows
were already `discoverable`, in the heal/refresh scope, not new-word shipping).
