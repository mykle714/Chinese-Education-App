# Oracle backfill run — 2026-08-26T06:27:34Z

- Mode: solo/unsharded, `--words=` scoped batch (20 zh words), lang=zh
- Backup: taken at round start per skill §2 (see `server/backups/` for the dump from this session)

## Batch

内卡河, 内地, 内城, 内场, 内外, 内定, 内心, 内情, 内战, 内河, 内流, 内流河, 内海, 内用, 内皮, 内眼角, 内科, 内科学, 内行, 内里

## Scripts run (manifest order, all oracle export→apply)

1. `backfill-long-definitions` (v15) — done in prior segment of this session
2. `backfill-longdef-citations` (v2) — done in prior segment, 0 needed
3. `backfill-example-sentences` (v6) — all 20 words generated/validated; 内外 needed one Opus repair (`regenerateSentenceWithOpus`) plus its own delayed stage-4 tagging calls
4. `backfill-classifier` (v2) — 20/20 updated. 6 words assigned a measure word: 内卡河→条, 内城→座, 内战→场, 内河→条, 内流河→条, 内海→片. Remaining 14 assigned `[]` (no dedicated classifier).
5. `backfill-breakdown-senses` (v1) — 19/19 oracle-disambiguated + 内战 auto-resolved without an oracle call = 20/20.
6. `backfill-breakdown-elaboration` (v1) — 20/20 judged: 2 elaborated (内卡河, 内行), 18 straightforward (written NULL).

`oracle-plan.js --words=<batch>` re-run after step 6 confirmed **0 prompts across 0 scripts** — nothing left pending.

## Promotion

`promote-discoverable.js --words=<batch> --apply` — 20/20 promoted, 0 not-ready, 0 already-discoverable.

## Verification (§5)

- Per-word non-null check (`partsOfSpeech`, `longDefinition`, `exampleSentences`, `frequencyScore`, `breakdown`) — all 20 rows pass, all `discoverable = true`, difficulty 4–6.
- Corpus-wide `discoverable_without_level` (difficulty NULL or out of 1–6 range among discoverable rows) — **0**.
- Validator-overwrite guard (validations rows with action IN approve/flag against this batch's ids) — **0 rows**.

## Flags for review

- **⚠ BREAKDOWN SENSE REVIEW 内卡河**: the 卡 character has no semantic candidate that fits — it is a phonetic transliteration syllable in "Neckar River" (内卡河 = Neckar River in Germany), not a meaning-bearing morpheme. Downstream, `backfill-breakdown-elaboration` correctly flagged the resulting breakdown ("inside + card + river") as non-straightforward and supplied a short elaboration noting 卡 is used only for its sound.
- 内行's breakdown ("inside + trade") was also judged non-straightforward — "expert" requires the insider/outsider metaphor (cf. 外行 "outsider/layman"), not a literal reading. Elaboration supplied.
- Carried over from a prior segment (`backfill-cluster-definitions.js`): **⚠ CLUSTER REVIEW 内地** — flagged an uncertain "Japan (colonial-era Taiwan usage)" sense during clustering; no further action taken this round beyond what that earlier stage already recorded.

## Session notes

- This was a single round per the invoking cron ARGUMENTS (unsharded, solo, stop after one round). No second `--new`/`--discoverable` batch was pulled.
- `server/logs/oracle-resume.md` was not written to — no parking was needed this round.
