# Oracle backfill run — 2026-08-27T11:38:02Z

Mode: solo, unsharded, one round, autonomous cron invocation.

## Backup

`server/backups/det-20260827T101721Z-oracle-cron-20260827T101721Z.sql.gz`
Restore: `gunzip -c <path> | psql ... cow_db` (see `backup-det.sh` header for the
full restore invocation).

## Scope

25-word zh `--new` batch (candidates ranked by planner's corpus-commonness ordering,
all sharing the 为/好/起 character cluster):

学好, 安好, 安好心, 对得起, 小起重机, 平地起家, 心眼好, 心头好, 成为, 有为, 正好,
为, 为上, 为主, 为人, 为数, 为时, 为重, 无为, 无为市, 特为, 特起, 性行为, 治好, 为生

## Manifest steps run (in dependency order)

| Script | Scope (words) | Result |
|---|---|---|
| backfill-tones | 25 | applied (from earlier session segment) |
| backfill-numbered-pinyin | 25 | applied (from earlier session segment) |
| backfill-long-definitions | 25 | applied — **2-stage pipeline confirmed**: generator → validator, per-word |
| backfill-longdef-citations | 25 | applied (from earlier session segment) |
| backfill-cluster-definitions | 25 | applied — **3-stage pipeline confirmed**: cluster proposal → per-cluster reading → per-cluster frequency score. 4 self-flagged `⚠ CLUSTER REVIEW`: 有为, 正好, 为, 为人 |
| backfill-example-sentences | 25 | applied — **3-stage pipeline confirmed**: sentence generation → grammar/naturalness review → per-sentence segment tagging (3 rounds of 25) |
| backfill-classifier | 6 (nouns only: 小起重机, 心头好, 性行为, 为人, 无为, 无为市) | applied, single-stage. Only 小起重机 → 台, all others → [] |
| backfill-breakdown-senses | 24 (all multi-char, excl. 为) | applied, single-stage. 3 self-flagged review notes: 学好 (好), 对得起 (起), 心眼好 (眼) — no candidate label matched the idiomatic/metaphorical sense in use, closest literal label kept |
| backfill-breakdown-elaboration | 24 (all multi-char, excl. 为) | applied, single-stage. 11 elaborated / 13 straightforward (NULL) / 0 failed |

Session budget: checked at preflight (earlier session segment); no window boundary
crossed mid-round.

## Promotion

`promote-discoverable.js --words=<25>` dry-run then `--apply`: **25/25 promoted**,
0 not-ready, 0 already-discoverable.

## Verification (§5)

All 25 rows: `partsOfSpeech`, `longDefinition`, `exampleSentences`, `frequencyScore`
non-null; `discoverable = TRUE`; `difficulty` in [3,6]. `为` (single-char) correctly
shows no `breakdown` — that step does not apply to 1-char words. No validator-approved
or -flagged field was touched by this round (none of these 25 ids had prior
validator activity).

## Review flags for follow-up

- Cluster-definition self-flags (uncertain sense split/ranking): 有为, 正好, 为, 为人
- Breakdown-sense self-flags (no candidate label matched the in-context sense):
  学好 (好: complement-of-result use), 对得起 (起: metaphorical "afford" sense in
  V-得-起), 心眼好 (眼: idiomatic "心眼"=intention has no matching literal-eye label)

## Content policy

No drops this round — `性行为` ("sexual behavior") is standard clinical/dictionary
vocabulary per skill §3c, not a drop case; treated as ordinary content throughout
(classifier, breakdown, example sentences, elaboration all authored neutrally).

**Recurring bug, still open** (see `server/logs/oracle-concerns.md`,
2026-08-26T21:19:00Z entry): 阿三/小日本/台巴子/老黑/日人民报 (ids 113290, 34323,
91290, 87932, 52122) keep resurfacing at the top of every `--new` batch because they
share very common characters with the corpus-commonness ranking, and get re-dropped
every round with no durable exclusion. This round's batch did not include them, but
the underlying gap (no denylist the planner's `--new` scope reads) is still
unaddressed.

## Stop condition

Single round complete per instruction — not starting a second round.
