# Oracle run — 20260822T161818Z (HALTED at preflight, no rows written)

Autonomous cron round, solo/unsharded. **No enrichment was performed.** The run stopped
in §1 preflight on a budget guardrail, before §2 backup and before any prompt was exported.

## Session budget

| Window | utilization before | utilization after | rounds | notes |
|---|---|---|---|---|
| five_hour | 0.0% | 0.0% (preflight only) | 0 | `resets_at: null` — no window was open |
| **seven_day** | **100.0%** (`severity: critical`, `is_active: true`) | 100.0% | — | resets **2026-08-24T00:59:59Z** |

**Guardrail that ended the run: the weekly plan budget is fully spent.**

`five_hour` reads 0% and would let a round start, but `weekly_all` is at 100% and active.
With the plan allowance gone, every token this round would author bills to
`extra_usage` (already **$256.63** of credits consumed; `is_enabled: true`,
`spend_limit_reached: false`, so requests still succeed — they just cost money).

That inverts the premise of this skill, which exists to run the pipeline *without*
paid inference. Spending real credit is not reversible and this round is unattended,
so it was not a call to make without the user. Deferring is reversible: nothing is
half-written, and the work is still there on 2026-08-24.

**This is not the `five_hour` window-boundary case (§6a).** There is no batch in
flight, so there is nothing to park — no resume note was written, deliberately.
The next cron tick after **2026-08-24T01:00Z** should start a normal fresh round.

## Backup (§2)

Not taken — the run halted before §2. No prod writes occurred, so none was needed.

## Words

None. No batch was planned, no prompt exported, no answer authored, no row updated
or promoted.

## Checks that were run (read-only, no DB writes)

- `check-manifest-sync.js` → **✅ all 24 zh + es manifest versions match their
  `SCRIPT_VERSION`.** No drift in the dangerous direction (script ahead of manifest),
  so the planner will report staleness correctly whenever the next round runs.
- Parked-run check → `server/logs/oracle-resume.md` absent. No previous window left a
  run mid-flight; `server/logs/oracle-run-notes.md` carries only completed-round notes
  from rounds 71–74 (2026-08-19/20).
- Corpus counters (`dictionaryentries_zh`, `language='zh'`):

  | | count |
  |---|---|
  | `discoverable` | **5,540** |
  | corpus total | **114,768** |
  | never shipped | 109,228 |

  Unchanged by this round.

## Guardrail evidence

No `UPDATE` was issued against `dictionaryentries_zh` / `dictionaryentries_es` /
`validations`, so no validator-approved or flagged field could have been touched, and
no row's `discoverable` flag moved. The §5 verification SQL was not run because there
was no write window to verify.

## Anything uncertain

- **Whether halting was the right call is a judgment, not a rule.** The skill's §6 stop
  list does not name an exhausted *weekly* budget — it is written around the five-hour
  window, which §6a says to ride through rather than treat as an ending. The weekly
  limit is different in kind: crossing it does not stall, it starts charging. §6b is the
  only place the doc looks at `seven_day`, and it does so to say more workers "buy
  nothing" near 100% — the same reasoning applied here to a single worker.
- **If the user is happy to run on overage credits**, this halt is unnecessary and the
  cron should be told so — the cleanest fix is an explicit instruction in the
  `oracle-cron.sh` invocation (or a note in this skill) stating that `seven_day` at
  100% is *not* a stop condition. Absent that, an unattended round will keep deferring
  until 2026-08-24.
- Findings carried forward from the last completed rounds are still open and unaddressed
  (see `server/logs/oracle-run-notes.md`): example-sentences shipping duplicate
  sentences after the per-POS top-up stage, word-forms deriving every POS form from the
  first definition only, and the planner's `--new` curation still surfacing explicit
  headwords and cedict place-name stubs. None were touched this round.
