# Oracle backfill run report — 2026-08-26T10:22:19Z

Autonomous cron round (solo, unsharded — no `--shard`). Single round per invocation
instruction: complete one pass through §3→§5, then stop (no §6a parking loop,
no second `--new` batch).

## Backup
`server/backups/det-20260826T091718Z-oracle-cron-20260826T091718Z.sql.gz`

## Scope
zh `--new`, planner batch of 25 candidates → 20 processed after content-policy drops.

Word batch (20): 城阳, 城阳区, 报关, 大内, 大出血, 大关, 大关县, 安阳市, 安阳县,
对内, 山海关, 山海关区, 山阳, 山阳区, 山阳县, 市内, 平阳, 平阳县, 年内, 年关

## Content policy drops (§3c)
5 words dropped from the planner's candidate set before authoring began — primary
sense is an ethnic/racial slur: 阿三, 小日本, 台巴子, 老黑. One additional word,
日人民报, was dropped pending review as a likely mistranscription (not a policy
drop). Logged in `server/logs/oracle-concerns.md`; not processed this round, and
per the invocation instruction not picked up for a follow-on `--new` batch either.

## Scripts run (manifest order)
tones/numbered-pinyin (deterministic, `--stale`) → dictionary-breakdown →
process-definitions-array → parts-of-speech → word-forms → hsk-level →
frequency-score → cluster-definitions → long-definitions → longdef-citations →
example-sentences → classifier → breakdown-senses → breakdown-elaboration →
promote-discoverable.

Full per-script detail, including two discoveries worth carrying into future
rounds, is in `server/logs/oracle-run-notes.md` (this cron worker's dedicated
notes path, not the skill's default):

1. **Deterministic-script trap**: `backfill-tones`/`backfill-numbered-pinyin`
   are `deterministic: true` in the manifest (no LLM calls) and gate their own
   row selection on `column IS NULL`, while the planner/promoter gate on the
   `enrichmentLog` stamp. A script reporting "0 entries needing backfill" does
   not mean the planner will treat the row as done — always follow with
   `--stale` for these two steps before trusting a "0 pending" reading.
2. **`backfill-example-sentences.js` is a 6-stage cascade**, not one prompt:
   generate → critic → per-sentence segmentation, doubled for any word needing a
   second/third sense's example after clustering settled. 大出血/大关/山阳 each
   needed a regenerated-sense round.

## Content-policy handling
5 drops (slurs), 0 explicit-sexual-content drops. See `oracle-concerns.md`.

## Verification (§5)
Run directly against prod (`cow-postgres-prod`, role `cow_user`, db `cow_db`):

- Null-enrichment-column check across all 20 words (`partsOfSpeech`,
  `definitions`, `longDefinition`, `exampleSentences`, `frequencyScore`, `tone`,
  `numberedPinyin`, `difficulty`): **0 nulls found**.
- Validator-guard check (`validations` table): **0 rows** — none of the 20 words
  were ever touched by a validator, so no approved/flagged field was at risk.
- Discoverable-without-valid-difficulty sweep: **0 rows**.
- `discoverable = TRUE` confirmed on all 20/20 words.

## Result
20/20 words promoted to `discoverable = TRUE` via `promote-discoverable.js --apply`
(never via hand-written SQL). No rows rejected by the promoter. No manifest
version drift (`check-manifest-sync.js` clean, 24/24 steps, at round start).

## Stop condition
Per the invoking instruction, stopping here after exactly one round. Not
processing the 阿三/小日本/台巴子/老黑/日人民报 candidates surfaced by
`oracle-plan.js --new`, not entering §6a window-boundary parking.
`server/logs/oracle-resume.md` is empty — no parked state to clean up.
