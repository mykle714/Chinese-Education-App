# Oracle backfill run — 2026-08-24T18:37:38Z

Solo, unsharded cron round. Answerer: Claude (oracle mode), no live API spend.

## Session budget snapshot
Not re-checked at the top of this specific segment (checked earlier in the same
overall session before this batch's export/author/apply cycles began); no
`five_hour` or `weekly_all` cap was hit during this round — every script converged
without a park/resume event.

## Backup
`server/backups/det-20260824T171728Z-oracle-cron-solo.sql.gz` (17M)
Restore: `gunzip -c <path> | docker exec -i cow-postgres-prod psql -U cow_user -d cow_db`

## Manifest sync
`check-manifest-sync.js` — clean, no drift.

## Scope
Planner `--new --limit=25` candidates: 25. 5 dropped for content policy, 20 enriched
and promoted `discoverable = TRUE`.

## Newly discoverable / refreshed (20 words)
乐清市, 正式, 气动式, 水清无鱼, 法式, 清, 清一色, 清人, 清代, 清城区, 清场, 清新,
清新区, 清新自然, 清明, 清正, 清水, 清水县, 清水河县, 清河区

All newly shipped (none were previously discoverable). Difficulty range 4–6, all
within the [1,6] promotion bar.

## Per-script prompt/answer/update/reject counts (this segment; earlier manifest
steps for this batch were completed in prior segments of the same round)

| Script | Prompts | Answers authored | Updated | Rejected/undecided |
|---|---|---|---|---|
| `backfill-example-sentences.js` (stage 5/5: sentence-index-2 segment labeling) | 20 | 20 | 20 | 0 |
| `backfill-classifier` | 16 | 16 | 16 | 0 |
| `backfill-breakdown-senses` | 19 | 19 | 19 | 0 |
| `backfill-breakdown-elaboration` | 19 | 19 | 19 (2 elaborated, 17 straightforward) | 0 |

`backfill-example-sentences.js` overall (across all segments of this round) was a
confirmed 5-stage chain per row: batch sentence generation → sentence review →
per-sentence segment-labeling ×3. Fully converged (0 new captured prompts on the
final re-run).

Final planner re-check (`oracle-plan.js --words=<20-word batch>`) returned 0 prompts
across 0 scripts — manifest fully clear before promotion.

`promote-discoverable.js --apply` — 20/20 promoted, 0 not-ready.

## Content-policy drops (this round)
阿三 (id 113290), 小日本 (id 34323), 台巴子 (id 91290), 老黑 (id 87932),
日人民报 (id 52122) — logged to `server/logs/oracle-concerns.md`. All five are
primary/only-sense ethnic/national slurs or a vulgar pun-insult; left un-enriched
and non-discoverable. This exact set has recurred across multiple cron rounds today
since the planner's `--new` ranking is stable and every round independently
re-derives the same exclusion — not a new finding, just consistent behavior.

## Guardrail verification (§5)
- `validations` table: 0 rows for any of the 20 words — no validator-approved/flagged
  field existed to protect or overwrite; confirmed none were touched.
- All 20 rows: `partsOfSpeech`, `definitions`, `longDefinition`, `exampleSentences`,
  `frequencyScore`, `definitionClusters` all non-null; `discoverable = TRUE`;
  `difficulty` in [1,6]. Verified by direct SQL query post-promotion (see
  `oracle-run-notes.md` for the exact statements).

## Sample authored content
- **`backfill-classifier`**: 清人 (Qing-dynasty person, a person-noun) →
  `["位","个"]`; every other word (place names, abstract/idiomatic terms) → `[]`.
- **`backfill-breakdown-senses`**: 清人's 清 disambiguated to "Qing dynasty" (not
  "clear/pure/clean") given the word's meaning; self-flagged review note on 清新自然's
  自 → "naturally" (uncertain vs. "oneself"/"from, since").
- **`backfill-breakdown-elaboration`**:
  - 水清无鱼 (idiom) → *"A proverb: water too pure holds no fish, so demanding total
    purity in people drives them all away."*
  - 清明 → *"Named for the clear, bright weather of early spring, when families sweep
    ancestral tombs."*
  - All other 17 words judged straightforward (glosses compose to the meaning without
    elaboration).

## Uncertainties / judgment calls (not validator-confirmed)
- **清 = "clear/pure/clean" vs "Qing dynasty"**: assigned per-word by my own judgment
  across this batch — place names (乐清市, 清城区, 清新区, 清水河县, 清水县, 清河区,
  清水, 清新, 清新自然) got "clear/pure/clean" (reasoned as "clear and X" place-naming
  imagery, cf. 清远市); words explicitly about the dynasty (清人, 清代) got "Qing
  dynasty". Not sourced from an authoritative etymology reference.
- The recurring earlier-round conventions (的-disambiguation, verb-nominalization
  tagging, transliteration-fragment handling) continued to apply where relevant but
  produced no new edge cases this segment.
- 清新自然's 自-character sense (see above) is explicitly self-flagged as uncertain.

## Content-policy drop count
5 words dropped this round (see above); running log in
`server/logs/oracle-concerns.md` (append-only, not truncated — accumulates across
every round today, so it shows this set re-dropped by several earlier rounds too).

## Stop condition
Round complete: manifest cleared, 20/20 promoted, guardrails verified, run notes
corrected, this report written. Per explicit instruction, stopping here — not
starting a second round.
