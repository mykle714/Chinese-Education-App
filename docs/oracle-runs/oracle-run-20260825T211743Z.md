# Oracle backfill run — 2026-08-25T21:17:43Z

Autonomous cron round (solo), unsharded. Backup label `cron-solo-20260825`.

## Scope

Discoverable/new backlog batch of 20 zh words, all sharing the 运 character:
学运, 时来运转, 时运, 机运, 民运, 水上运动, 水运, 河运, 海运, 发运, 空运, 红运,
美国运通, 转运, 运, 运交, 运作, 运出, 运力, 运动员.

## Backup

`server/backups/det-20260825T211743Z-cron-solo-20260825.sql.gz` (dictionaryentries_zh,
dictionaryentries_es, validations). Taken before any write this round, per skill §2.

## Scripts run (manifest order)

1. `backfill-tones` (native)
2. `backfill-numbered-pinyin` (native)
3. `backfill-parts-of-speech` (oracle)
4. `backfill-hsk-level` (oracle)
5. `backfill-word-forms` (oracle)
6. `backfill-frequency-score` (oracle)
7. `backfill-cluster-definitions` (oracle)
8. `backfill-dictionary-breakdown` (native, 19/20 — skips single-char 运)
9. `backfill-long-definitions` (oracle)
10. `backfill-longdef-citations` (native)
11. `backfill-example-sentences` (oracle) — **~7 export/apply cycles**
12. `backfill-classifier` (oracle) — 2 batches (17 + 2 stragglers)
13. `backfill-breakdown-senses` (oracle, 19 words)
14. `backfill-breakdown-elaboration` (oracle, 19 words) — 1 length-retry cycle

Re-ran `oracle-plan.js --words=<batch>` after each script; final check returned
"0 prompt(s) total across 0 script(s)" — full manifest convergence confirmed before
promotion.

## Multi-cycle process notes

- **example-sentences** was the deepest chain this round: two words (民运,
  水上运动) had their original sentences flagged/rejected and needed THREE
  successive rounds of brand-new replacement sentences (not variants of the same
  sentence) before stabilizing. Every other word in the batch converged on cycle 1.
- **classifier** needed a second, 2-word straggler batch (美国运通, 运) that were
  not part of the initial 17-word planner output — they only became eligible once
  `backfill-breakdown-senses`/`backfill-breakdown-elaboration` ran and satisfied
  their POS/nounPos gate. Confirms the standing lesson: **always re-run the planner
  after each script converges**, not just once per round.
- **breakdown-elaboration length-retry** — a newly-confirmed sub-pattern distinct
  from the tagger/repair cycle already documented from prior rounds. When an
  authored elaboration exceeds its per-word character budget (which scales with
  breakdown length — roughly 50 chars for a 2-character word, up to 150 for a
  3-segment word), export re-captures the SAME word with an explicit correction
  instruction embedded in the prompt ("your previous answer was N characters —
  rewrite in at most M"). 民运's first elaboration (102 chars) was rejected against
  a 50-char limit; the retry ("Abbreviates 民主运动 'democracy movement'.", 38
  chars) succeeded.

## Elaboration outcome (verified against `dictionaryentries_zh."breakdownElaboration"`)

7 of 19 breakdown-applicable words received an elaboration (the "genuinely opaque"
bar, correctly the minority case):

| word | elaboration |
|---|---|
| 学运 | abbreviates 学生运动 'student movement'; 运 carries 运动's sense |
| 时来运转 | fixed idiom, non-literal |
| 民运 | abbreviates 民主运动 'democracy movement' |
| 水上运动 | 运动 means 'sport/exercise' here, not transport |
| 红运 | 红 = cultural good-fortune signal, not literal color |
| 美国运通 | 运通 chosen for sound/sense of 'Express', not literal translation |
| 运动员 | 运动 = 'sport/physical activity'; 员 = the person |

The remaining 12 (时运, 机运, 水运, 河运, 海运, 发运, 空运, 转运, 运交, 运作, 运出,
运力) were judged `straightforward: true`. 运 (single character) has no breakdown
row at all — `backfill-dictionary-breakdown`'s applicability gate skips single-char
headwords.

## Classifier outcome

Only 运动员 ("athlete", a count noun) received a non-empty classifier:
`["名","位","个"]`. All other 19 words — verbs, abstract nouns, an idiom, and a
proper noun — correctly received `[]` (no dedicated measure word).

## Review flags (self-flagged, non-blocking)

- `backfill-breakdown-senses` reviewNotes: 民运, 红运, 运作 — uncertain
  character-sense assignment in context, answered anyway per the skill's "don't
  block on uncertainty" instruction.

## Content-policy drops (§3c)

None in this 20-word batch. The recurring 5-word poison list (阿三, 小日本, 台巴子,
老黑, 日人民报) surfaced again during `--new` scope polling earlier in this window
(logged in `oracle-concerns.md` at 21:19:21Z / 21:31:17Z, consistent with 30+ prior
rounds) but is unrelated to this batch — no new drops to log here.

## Promotion

`promote-discoverable.js --words=<20-word batch>`:
- Dry run: Considered 20 | Would promote 20 | Already discoverable 0 | Not ready 0
- Applied: Considered 20 | Promoted 20 | Already discoverable 0 | Not ready 0

All 20 words are now `discoverable = TRUE` in production, difficulty range 2–6.

## §5 Verification (ad hoc SQL via `docker exec cow-postgres-prod psql -U cow_user -d cow_db`)

- Per-word non-null check (partsOfSpeech, longDefinition, exampleSentences,
  frequencyScore, definitionClusters) across all 20 rows: **20/20 pass**.
- `breakdown` non-null: 19/20 (运 correctly excluded — single character).
- `classifier` stamped (present, possibly `[]`): 19/20 (时来运转 correctly
  excluded — idiom, not a noun).
- `discoverable = TRUE AND difficulty BETWEEN 1 AND 6`: **20/20 pass**.
- `enrichmentLog` oracle stamps: spot-checked on 运动员 — every oracle-answered
  step (hsk-level, classifier, word-forms, frequency-score, parts-of-speech,
  breakdown-senses, long-definitions, example-sentences, cluster-definitions,
  breakdown-elaboration) carries `"oracle": true`.
- Validator-overwrite guard: not applicable — all 20 are brand-new promotions with
  no prior `validations` rows to protect.

## Budget

`five_hour.utilization` = 7% (window resets 2026-08-26T03:09:59Z).
`weekly_all` = 43%. Both well clear of any stop threshold — no park, no
`oracle-resume.md` state to hand off.

## Stop

Per this round's ARGUMENTS ("stop cleanly at the end of one round; do not start a
second"), the round ends here.
