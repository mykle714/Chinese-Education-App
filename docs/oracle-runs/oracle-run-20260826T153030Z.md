# Oracle backfill run — 2026-08-26T15:30:30Z

Solo, unsharded cron round. Round notes: `server/logs/oracle-run-notes.md`
(round section "20-word zh batch (血流/关 cluster) — 2026-08-26").

## Scope

`dictionaryentries_zh`, 20 words newly shipped this round:
血流, 血流成河, 血清, 血球, 血管, 血色, 血路, 通关, 过关, 重阳, 金阳县, 开关,
开阳, 开阳县, 关, 关上, 关中, 关中地区, 关内, 关公.

Backup: `server/backups/det-20260826T141722Z-oracle-cron-solo-20260826T141722Z.sql.gz`.

## Manifest steps run

`example-sentences`, `classifier`, `breakdown-senses`, `breakdown-elaboration` — the
zh required-scripts manifest tail still pending on this batch (earlier manifest
stages — POS, definitions, frequency score, clusters, tone/pinyin/breakdown base —
were already current or covered by an earlier stage of this same round; see the
round-notes section for the full per-script breakdown).

All four steps converged 20/20 (19/19 where a step is multi-char-only), 0 unresolved
failures. Two entries required a length-conscious retry on `breakdown-elaboration`
(重阳, whose first answer exceeded the script's stated character hard limit — the
script's own retry mechanism re-prompted with a tighter limit rather than failing the
row outright).

## Promotion

`promote-discoverable.js --apply`: 20/20 promoted, 0 not-ready, 0 already-discoverable.

## Verification (skill §5)

0 rows with a null required enrichment column across the batch (single-char 关
correctly has no `breakdown` — not applicable to 1-char words). 0 rows discoverable
without a valid `difficulty` (range 1–6). No pre-existing `validations` rows for any
of the 20 ids, so the validator-approved-field guard was not a factor this round.

## Content policy

No drops. All 20 words are ordinary vocabulary — physiology/blood terms,
customs/pass-through compounds, place names, a solar-calendar festival name, and a
historical figure's name — none triggered the slur/explicit-content policy.

## Status

Round complete. Per the standing cron instruction (solo, unsharded, stop after one
round), no second batch was started this invocation.
