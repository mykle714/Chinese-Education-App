# Oracle run — ABORTED AT PREFLIGHT (no work performed)

Invocation: autonomous cron round, solo/unsharded, "stop cleanly at the end of one round".

## Outcome
The round never started. Nothing was enriched, nothing was promoted, no backup
was taken, the planner was never invoked, and no DB write of any kind occurred.

## Session budget

| Window | Utilization before | Utilization after | Rounds | Park |
|---|---|---|---|---|
| five_hour | 0.0% (`resets_at: null`, no window open) | 0.0% (unchanged — no requests billed to it) | 0 | n/a |
| seven_day | **100.0%** (`critical`, resets `2026-08-24T01:00:00Z`) | 100.0% | 0 | n/a |

`extra_usage`: enabled, `used_credits` 33636 (= $336.36), `spend_limit_reached: false`,
`monthly_limit: null`.

## Which guardrail ended the run
Not one of §6's four literal stop conditions — this is a preflight refusal, and it is
worth stating plainly rather than filing under an existing bullet.

The five-hour session window was completely empty (0.0%), so §6/§6a did not apply: there
was no boundary to park at. The binding constraint was the **weekly** pool at 100%. Past
that limit the plan no longer covers requests; they bill to extra-usage credits instead.
Oracle mode exists specifically to run the enrichment pipeline *without* spending API
credit, so a round started here would invert the skill's own premise, and would do so
unattended with no human present to authorize the spend. §6b already reasons this way
about parallel workers ("check `seven_day` … if it is near 100%, more workers buy
nothing"); the same arithmetic applies to the solo worker.

## Backup
None taken (§2 is only reached once a round is actually going to write). No restore
command applies.

## Words / per-script / A1.5 / cluster review / guardrail evidence / sample content
All empty — no scripts ran. Since no row was touched, no `validatedClause`-protected
field could have moved, and the §5 verification SQL was not run (it would only re-assert
a state this round did not change).

## Anything uncertain / for a human
- **Resuming**: safe unattended after `2026-08-24T01:00:00Z`, when the weekly pool resets.
  Before then, a round should only run on an explicit instruction to spend extra-usage
  credits.
- **Worth deciding**: `extra_usage.monthly_limit` is `null` and `spend_limit_reached` is
  `false`, meaning nothing mechanically stops an unattended round from spending. If cron
  rounds are meant to be strictly plan-budget-only, the cheap fix is a `seven_day >= 100`
  preflight abort inside `server/scripts/backfill/oracle-cron.sh`, alongside the
  `check-manifest-sync.js` drift abort it already does — that check is the right precedent,
  and it would make this refusal automatic rather than dependent on the answerer noticing.
- No parked-run note was written (`server/logs/oracle-resume.md` does not exist and should
  not) — there is no in-flight batch to resume. The next invocation should plan a fresh
  round.
